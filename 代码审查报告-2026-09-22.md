# 代码审查报告：丹邱丝苗米普惠产融服务平台

- 审查日期：2026-09-22
- 审查范围：远程 `main`（4779352）相对本地上次提交（7ec57cd）的全部合作者改动（16 个提交，97 文件，+9721/-585）
- 审查对象：FastAPI 后端、H5 前端（frontend）、管理后台（admin-web）、政府大屏（bigscreen）、Docker 部署配置、SQL 与迁移
- 审查方法：逐文件人工审查 + 全库关键词扫描 + 静态检查 + 测试运行

## 一、总体结论

代码整体质量**中等偏上**：工程结构清晰（角色守卫集中挂载、业务规则单点定义、SQL 全参数化、无硬编码密钥入库），测试与静态检查干净。但存在 **2 个高危问题**（订单发货越权、认证体系为演示强度）和若干中等/低危问题。上线前必须修复高危项并替换默认密钥。

## 二、验证结果

| 检查项 | 结果 |
|---|---|
| ruff 静态检查 | ✅ 全部通过 |
| 单元测试（rules/db 归一化等） | ✅ 11/11 通过 |
| 集成测试（需数据库） | ⏭️ 23 个自动跳过（本机未启动 MySQL/API） |
| 已提交密钥扫描 | ✅ 未发现真实密钥（仅 .env.example 开发默认值） |

## 三、高危问题（上线前必须处理）

### H1. 订单发货接口越权（越权操作任意订单）
- **位置**：`backend/routers/shop.py` — `ship_order()`（PUT `/api/shop/orders/{order_id}/ship`）
- **问题**：shop 路由在 `app.py` 中**无守卫挂载**，且 `ship_order` 函数内部**没有角色校验**（同文件的 `all_orders` 有 `OperatorUser(user)` 校验，此处遗漏）。任何登录用户（包括普通消费者）都能给任意订单发货、伪造物流单号，绕过运营审批流程。
- **修复**：函数开头加 `OperatorUser(user)`，或将该接口拆分到受守卫的路由。

### H2. 认证体系为演示强度，等于"人人可登录任意账号"
- **位置**：`backend/config.py`、`backend/routers/auth.py`、`docker-compose.yml`
- **问题**：
  1. JWT 默认密钥 `danqiu-dev-secret-change-me`（config.py 与 docker-compose 都是默认值），知道代码的人可伪造任意角色 token（含 ADMIN）；
  2. 验证码写死 `1234`，且 `/api/auth/captcha` 直接把验证码返回给前端（`dev_mode: True`）；
  3. README 公开了各角色演示手机号。
  以上组合 = 任何人可用公开手机号 + 1234 登录任何角色账号。属于 PRD 明确标注的 MVP 边界，**仅限演示**。
- **修复**：部署时强制设置随机 `DANQIU_JWT_SECRET`；上线前接入真实短信通道、删除 `dev_mode` 返回、增加登录限流与失败锁定。

## 四、中等问题

### M1. 公开接口泄露个人信息
- **位置**：`backend/routers/overview.py`（`/api/farmers`、`/api/orders`、`/api/loans`、`/api/claims`）、`backend/routers/government.py`（`/api/government/dashboard`）
- **问题**：以上接口全部无鉴权公开，返回农户/消费者真实姓名、手机号、贷款金额、保单、订单等信息。大屏聚合数字公开可以理解，但明细接口建议收回鉴权。
- **修复**：明细接口加登录/角色守卫；公开接口只保留聚合指标。

### M2. 跨地块订单分红错配
- **位置**：`backend/routers/admin.py` — `calculate_dividends()`
- **问题**：多商品订单只取第一个商品的地块（`LIMIT 1`），整单分红记给该地块农户；订单含多个地块商品时，分红归属错误。
- **修复**：按订单明细逐地块分摊，或限制一单一地块并在下单时校验。

### M3. Token 存 localStorage + 地址快照手拼 JSON
- **位置**：`frontend/src/api.js`、`admin-web/src/store/auth.js`、`backend/routers/shop.py`
- **问题**：JWT 存 localStorage（XSS 可窃取，MVP 可接受但需知晓）；下单地址用 f-string 拼 JSON，收货人/电话含引号时生成非法 JSON。
- **修复**：地址用 `json.dumps()`；有条件时改 httpOnly cookie + CSRF 防护。

### M4. 上传文件只校验扩展名，不校验内容
- **位置**：`backend/routers/uploads.py`
- **问题**：扩展名白名单 + 10MB 限制 OK，但未校验文件真实内容（magic bytes），伪装成图片的任意文件可上传，且 `/uploads` 静态目录公开可访问。
- **修复**：用 Pillow 解码验证或校验 magic bytes；非图片文件拒绝。

### M5. 错误信息泄露内部细节
- **位置**：`backend/db.py`（503 detail 含 MySQL 异常原文）、`backend/routers/overview.py` `/api/health`（返回数据库版本号）
- **修复**：对外只返回通用错误码，细节写日志。

### M6. 无数据库连接池
- **位置**：`backend/db.py`
- **问题**：每次查询/事务都新建 MySQL 连接，无池化；并发上来会耗尽连接。`autocommit=True` 全局开启又手写 begin/commit，风格不一致。
- **修复**：接入连接池（如 DBUtils/SQLAlchemy），统一事务语义。

### M7. 业务单号随机 4 位数碰撞
- **位置**：`backend/routers/farmer.py`、`consumer.py`、`insurance.py`、`shop.py`（`next_no` / `_next_order_no`）
- **问题**：`random.randint(1000,9999)` 生成单号，命中唯一索引时直接 503，量级上来后概率不可忽略。
- **修复**：时间戳 + 序列/短 UUID。

## 五、低危问题

| 编号 | 位置 | 问题 |
|---|---|---|
| L1 | `shop.py` create_order | 库存 check-then-update 非原子，并发可超卖；改为条件 UPDATE 并校验影响行数 |
| L2 | `admin.py` update_insurance_product、`shop.py` create_order | dict 取值直接 `float()`/`int()`，非法字符串触发 500；统一用 Pydantic 校验 |
| L3 | `auth.py` get_current_user | 签名有效但缺 `sub` 的 token 会 KeyError → 500（极小概率） |
| L4 | `government.py` _dynamics | 理赔记录未查时间字段（`time` 恒为 None），随后被 `if x.get("time")` 过滤掉，**理赔动态永远不会出现在大屏**（死代码） |
| L5 | `farmer.py` create_record | `photo_urls` 不校验归属与来源，可引用任意 URL/他人上传文件 |
| L6 | `README.md` | 开发路线"阶段 2"重复两行 |

## 六、做得好的地方

- **SQL 注入防护**：全部查询参数化，未发现 f-string 拼接 SQL。
- **权限设计思路正确**：角色守卫在 `app.py` 全局挂载 + 关键操作端内二次校验，大部分接口权限正确。
- **业务规则集中**：`rules.py` 单点定义保险/贷款/分红公式，Decimal 精度处理正确，且有单元测试覆盖。
- **写入流程规范**：事务包裹、状态机校验齐全（重复审批返回 409、终审不可再操作）。
- **上传安全基线**：扩展名白名单、大小上限、UUID 文件名防路径穿越。
- **测试与规范**：11 个单元测试全过、ruff 全绿；集成测试可自动跳过（无 DB 环境友好）。
- **敏感信息管理**：无真实密钥入库，.gitignore 正确覆盖 `.env`。

## 七、关于"金融模型 / 算法模型"的结论

**当前代码没有使用任何金融模型或算法模型。**

- 全部金融计算是固定参数的确定性公式（`backend/rules.py`）：保额=面积×1500、保费=保额×5%、补贴 80%、贷款建议=面积×800、分红=订单×5%、理赔=保额×受灾比例、风险分级=三条 if/else 规则。
- 全库无任何 ML/统计库 import（sklearn/torch/numpy/pandas/scipy 均无），后端依赖仅 FastAPI/PyMySQL/PyJWT，前端依赖仅 Vue/Vant/Element Plus。
- 数据库无利率、还款计划、期限等字段；"卫星影像"页面为演示占位（标注"演示数据，不可加载"）。
- README 明确 MVP 边界：真实放款、卫星实时计算、大模型等本期不做。

若要接入真实金融能力，建议后续补充：信用评分模型、贷款定价与还款计划测算、精算费率、或基于农事数据的产量预测。

---

*本报告基于当前 main 分支代码人工审查，未覆盖运行时行为（数据库未启动）。修复优先级：H1 > H2 > M1-M7 > L1-L6。*
